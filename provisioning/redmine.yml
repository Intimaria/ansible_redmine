---
- name: Setup System
  hosts: all
  become: true
  roles: 
    - system
    - user
    - db
    - apache
    - download_redmine


- name: Install Redmine
  vars:
    user_name: redmine
  hosts: all
  become: yes
  become_user: "{{ user_name }}"
  become_method: sudo
  roles: 
    - ruby
